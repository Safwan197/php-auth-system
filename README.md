# php-auth-system
A lightweight authentication system built with PHP, HTML, and Bootstrap — uses a MySQL database (via XAMPP &amp; phpMyAdmin) to store and manage user data, featuring signup, login, and session management.
